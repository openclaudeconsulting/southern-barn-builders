# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node / build artifacts (future-proofing)
node_modules/
dist/
build/
.cache/
.parcel-cache/

# Env / secrets
.env
.env.*
!.env.example

# OAuth / API credentials (live in parent dir but exclude defensively)
credentials.json
token.json
client_secret*.json
.discord_webhook
.discord_bot.env
.anthropic_api_key

# Generated quote PDFs (live in parent dir but exclude defensively)
INVOICE*.pdf
QUOTE*.pdf

# Netlify
.netlify/

# Cloudflare
.cloudflare/
.wrangler/

# Image backups (uncompressed originals kept locally only)
images/_original_backup/

# Logs
*.log
npm-debug.log*
.supervisor.lock
